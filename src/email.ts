import emailjs from "@emailjs/browser";
export type BookingEmail={parentName:string;parentEmail:string;studentName:string;grade:string;subject:string;tutorName:string;slotLabel:string;rate:number};
export async function sendBookingEmails(data:BookingEmail){const serviceId=import.meta.env.VITE_EMAILJS_SERVICE_ID,parentTemplate=import.meta.env.VITE_EMAILJS_PARENT_TEMPLATE_ID,ownerTemplate=import.meta.env.VITE_EMAILJS_OWNER_TEMPLATE_ID,publicKey=import.meta.env.VITE_EMAILJS_PUBLIC_KEY,ownerEmail=import.meta.env.VITE_DANA_EMAIL;if(!serviceId||!parentTemplate||!ownerTemplate||!publicKey||!ownerEmail)return{parent:"not_configured",owner:"not_configured"}as const;const shared={parent_name:data.parentName,parent_email:data.parentEmail,student_first_name:data.studentName,student_grade:data.grade,subject:data.subject,tutor_name:data.tutorName,session_time:data.slotLabel,rate:`$${data.rate}/hour`,dana_email:ownerEmail};const[parent,owner]=await Promise.allSettled([emailjs.send(serviceId,parentTemplate,{...shared,to_email:data.parentEmail},{publicKey}),emailjs.send(serviceId,ownerTemplate,{...shared,to_email:ownerEmail},{publicKey})]);return{parent:parent.status==="fulfilled"?"sent":"delayed",owner:owner.status==="fulfilled"?"sent":"delayed"}as const}
